---
# Add isolated network interface to VMs via libvirt
# This runs on localhost (hypervisor) to modify the VMs

- name: Process each VM to add isolated interface
  ansible.builtin.include_tasks: add_interface.yml
  loop: "{{ libvirt_vms }}"
  loop_control:
    loop_var: libvirt_vm
